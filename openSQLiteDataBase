--SQL Databases used
SELECT * FROM climbs;
SELECT * FROM climb_stats;

--scraping climb name, frame, *grade* but I cannot find the grade
SELECT uuid, name, frames FROM climbs;

--scraping angle, ascensionist_count, difficulty_average, quality_average
SELECT climb_uuid, angle, ascensionist_count, difficulty_average, quality_average FROM climb_stats;


